export interface ParamsProps {
  params: {
    id: string;
  };
}

export interface TemplateParamsProps {
  params: {
    templateId: string;
  };
}

export interface TemplateExerciseParamsProps {
  params: {
    templateId: string;
    exerciseId: string;
  };
}
